trigger:
  branches:
    include:
      - master

pool:
  name: Default
  demands:
  - agent.name -equals vikas

steps:
- script: |
    echo python --version
    python main.py
  displayName: 'Print Message'
