trigger:
  branches:
    include:
      - main

pool:
  name: Default

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.10.0'
    addToPath: true

- script: |
    python --version
    python main.py
  displayName: 'Print Message'
